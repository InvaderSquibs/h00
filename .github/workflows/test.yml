name: Test
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo Hello
      - name: Hello world action step
        id: hello
        uses: jloganolson/h01@v1.25
